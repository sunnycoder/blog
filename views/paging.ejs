<br><br>
<div>
	<% if (!isFirstPage) { %>
		<span class="prepage"><a title="pre page" href="?p=<%= page-1 %>">pre page</a></span>
	<% } %>	
	<% if (!isLastPage) { %>
		<span class="nextpage "><a title="next page" href="?p=<%= page+1 %>">next page</a></span>
	<% } %>
</div>
